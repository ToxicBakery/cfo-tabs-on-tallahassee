<?xml version="1.0" encoding="utf-8"?>
<com.ToxicBakery.app.tabsontallahassee.widget.WidthSquareRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                                        xmlns:tools="http://schemas.android.com/tools"
                                                                        android:layout_width="match_parent"
                                                                        android:layout_height="match_parent">

    <ImageView
        android:id="@+id/person_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        tools:ignore="ContentDescription"/>

    <FrameLayout
        android:id="@+id/person_name_bg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="?android:colorPrimaryDark">

        <TextView
            android:id="@+id/person_name"
            style="@style/TextAppearance.AppCompat.Large"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:lines="1"
            android:padding="7dp"
            android:singleLine="true"
            android:text="@string/app_name"
            android:textColor="?android:textColorPrimaryInverse"/>

    </FrameLayout>

    <View
        android:id="@+id/person_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:foreground="?selectableItemBackground"/>

</com.ToxicBakery.app.tabsontallahassee.widget.WidthSquareRelativeLayout>